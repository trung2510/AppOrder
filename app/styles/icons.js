import {StyleSheet} from 'react-native'
import {vScale, hScale} from '../commons/PerfectPixel'

export const styleIcons = StyleSheet.create({
    iconLogin: {
        width:hScale(20),
        height: vScale(20),
        resizeMode: 'stretch'
    }
})